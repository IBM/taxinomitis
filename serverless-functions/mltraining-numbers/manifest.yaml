packages:
  mltraining-numbers:
    version: 0.12
    actions:
      DescribeModel:
        docker: dalelane/mlforkids-numbers:0.0.3
        runtime: python
        web-export: true
    apis:
      mltraining-numbers:
        api:
          describe-model:
            DescribeModel:
              method: POST
              response: http
