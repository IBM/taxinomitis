declare module 'http-range-parse';

// declare module 'http-range-parse' {
//     export default function rangeParse(header: string): { unit: string, first: number, last: number };
// }

// export default function rangeParse(header: string): { unit: string, first: number, last: number };
